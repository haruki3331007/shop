<%= form_with model: @cart_item, url: end_user_cart_items_path(current_customer), method: :post  do |cart| %>
    <%= cart.hidden_field :item_id, value: @item.id %>
    <%= cart.select :amount, *[1..5], {include_blank:'個数選択'} %>
    <%= cart.submit "カートに入れる" %>
<% end %>


